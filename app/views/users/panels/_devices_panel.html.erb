<div class="flex flex-centered-h info-panel">
  <% if @user.devices.count.zero? %>

  You have no devices :(

  <% end %>
  <ul>
  <% @user.devices.each do |device| %>
    <li><%= device.name %></li>
  <% end %>
  </ul>
  <%= link_to 'Add Device', '#', class: 'btn btn-sm btn-primary' %>
</div>